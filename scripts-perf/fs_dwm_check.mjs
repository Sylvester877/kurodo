// DWM ground check: extended frame bounds of the Electron window vs the
// monitor. If the window rect says 0,0-1920,1200 but DWM exBounds differ,
// the window is virtually fullscreened but visually scaled.
Add-Type compile via powershell file.
